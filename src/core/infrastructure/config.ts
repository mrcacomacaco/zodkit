/**
 * @fileoverview Backward-compatible config re-export
 * Re-exports simplified config for backward compatibility
 */

export * from '../config';
export { configManager as ConfigManager } from '../config';